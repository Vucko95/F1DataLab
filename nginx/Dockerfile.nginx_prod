
# This Dockerfile no longer builds the frontend.
# It copies assets from the image built by the 'frontend' service.
FROM nginx:stable-alpine

# Copy static assets from the pre-built frontend image
COPY --from=f1datalab-frontend:latest /app/.next/static /app/.next/static
COPY --from=f1datalab-frontend:latest /app/public /app/public